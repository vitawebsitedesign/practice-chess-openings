const initialState = {
    colour: 'white'
};

const player = (state = initialState, action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
};

export default player;
